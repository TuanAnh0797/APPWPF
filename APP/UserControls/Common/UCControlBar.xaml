<UserControl x:Class="APP.UserControls.UCControlBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:APP.UserControls"
             mc:Ignorable="d" 
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             TextElement.Foreground="{DynamicResource MaterialDesignBody}"
             TextElement.FontWeight="Regular"
             TextElement.FontSize="13"
             TextOptions.TextFormattingMode="Ideal"
             TextOptions.TextRenderingMode="Auto"
             Name="MyUCControlBar"
             Background = "{DynamicResource MaterialDesignPaper}">
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="MouseLeftButtonDown">
            <i:InvokeCommandAction Command="{Binding MoveWindowCommand}" CommandParameter="{Binding ElementName=MyUCControlBar}"/>
        </i:EventTrigger>
    </i:Interaction.Triggers>
    <Grid>
        <materialDesign:ColorZone Padding="0">
            <Grid Margin="5">
                <StackPanel DockPanel.Dock="Right" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" Background="Transparent">
                    <StackPanel.Resources>
                        <Style TargetType="Button" BasedOn="{StaticResource MaterialDesignRaisedSecondaryButton}">
                            <Setter Property="Foreground" Value="White"></Setter>
                            <Setter Property="Height" Value="25"></Setter>
                            <Setter Property="Width" Value="40"></Setter>
                            <Setter Property="BorderBrush" Value="Transparent"></Setter>
                            <Setter Property="Margin" Value="5"></Setter>
                            <Setter Property="Padding" Value="0"></Setter>
                            
                        </Style>
                        <Style TargetType="Label">
                            <Setter Property="Foreground" Value="Black"></Setter>
                            <Setter Property="Height" Value="25"></Setter>
                            <Setter Property="Width" Value="200"></Setter>
                            <Setter Property="BorderBrush" Value="Transparent"></Setter>
                            <Setter Property="Margin" Value="5"></Setter>
                            <Setter Property="Padding" Value="0"></Setter>
        
                        </Style>

                    </StackPanel.Resources>

                    <Label Background="Transparent" HorizontalContentAlignment="Center" VerticalContentAlignment="Center">
                        <i:Interaction.Triggers>
                            <i:EventTrigger EventName="MouseLeftButtonDown">
                                <i:InvokeCommandAction Command="{Binding MoveWindowCommand}" CommandParameter="{Binding ElementName=MyUCControlBar}"/>
                            </i:EventTrigger>
                        </i:Interaction.Triggers>
                    </Label>
                    <Button  Command="{Binding MinimizeWindowCommand}" CommandParameter="{Binding ElementName=MyUCControlBar}"  Background="SteelBlue" Content="{materialDesign:PackIcon Kind=WindowMinimize}" />
                    <Button  Command="{Binding NormalWindowCommand}" CommandParameter="{Binding ElementName=MyUCControlBar}" Margin="5" Padding="0" Background="SteelBlue" Content="{materialDesign:PackIcon Kind=WindowRestore}" />
                    <Button  Command="{Binding CloseWindowCommand}" CommandParameter="{Binding ElementName=MyUCControlBar}" Margin="5" Padding="0" Background="Red" Content="{materialDesign:PackIcon Kind=CloseThick}" />
                   
                </StackPanel>
            </Grid>
        </materialDesign:ColorZone>
    </Grid>
</UserControl>
