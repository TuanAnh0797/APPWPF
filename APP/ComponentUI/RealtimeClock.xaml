<UserControl x:Class="APP.ComponentUI.RealtimeClock"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:APP.ComponentUI"
             mc:Ignorable="d">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>

    <Border Background="White"
            BorderBrush="#DDDDDD"
            BorderThickness="1"
            CornerRadius="12"
            Padding="30,0,30,0"
            Margin="0,5,0,5">
        <!--<Border.Effect>
            <DropShadowEffect Color="Gray" BlurRadius="2" ShadowDepth="3" Opacity="0.4"/>
        </Border.Effect>-->


            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="2*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!-- Date -->
                <TextBlock Text="{Binding CurrentDate, RelativeSource={RelativeSource AncestorType=UserControl}}"
                       FontSize="15"
                       FontWeight="SemiBold"
                       Foreground="Black"
                       HorizontalAlignment="Right"
                       VerticalAlignment="Center"
                       Visibility="{Binding ShowDate, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource BooleanToVisibilityConverter}}"
                       Grid.Column="0" Margin="0,0,10,0"/>


                <!-- Time -->
                <TextBlock Text="{Binding CurrentTime, RelativeSource={RelativeSource AncestorType=UserControl}}"
                       FontSize="18"
                       FontWeight="Bold"
                       Foreground="DarkSlateBlue"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       Grid.Column="1"/>

                <!-- Day of Week -->
                <TextBlock Text="{Binding CurrentDayOfWeek, RelativeSource={RelativeSource AncestorType=UserControl}}"
                       FontSize="15"
                       FontWeight="SemiBold"
                       Foreground="Black"
                       HorizontalAlignment="Left"
                       VerticalAlignment="Center"
                       Visibility="{Binding ShowDayOfWeek, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource BooleanToVisibilityConverter}}"
                       Grid.Column="2" Margin="10,0,0,0"/>
            </Grid>

    </Border>
</UserControl>
